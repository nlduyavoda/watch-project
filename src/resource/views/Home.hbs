<div class="mt-4">
    <div class="row">
                     {{#each products}}
                    <div class="col-sm-6 col-lg-4">
           
                                    <div class="card" >
                                        <a href="products/{{this.slug}}">
                                             <img class="card-img-top" src="{{this.images}}" alt="{{this.name}}">
                                             </a>
                   
                        <div class="card-body">
                             <a href="products/{{this.slug}}"><h5 class="card-title">{{this.name}}</h5></a>
                 
                            <p class="card-text">{{this.descrtipstion}}</p>
                          <a href="/{{products._id}}" class="btn btn-success" role="button">add to cart</a>
                            <a href="#" class="btn btn-primary">Buy it</a>
                        </div>
                        </div>
                     
                    </div>
           {{/each}}
    </div>
</div>